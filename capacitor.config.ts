import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'leitorPDFTCE',
  webDir: 'dist/leitorPDFTCE',
  bundledWebRuntime: false
};

export default config;
